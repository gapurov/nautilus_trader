# Unusual Whales Test Data

- `market_tide_response.json` is the `Daily Market Tide` example from the
  captured official OpenAPI source.
- `price_frame.json` is the `price:<TICKER>` frame from the captured official
  `PublicApi.SocketController.price` description.
- `join_ack.json` is the documented positive join acknowledgement from
  `PublicApi.SocketController.channels`.
- `malformed_response.json` and `malformed_frame.json` are marked synthetic
  malformed variants.

Source snapshot:
`resources/openapi.yaml`, SHA-256
`38ca3116dcbcc941c002154c02dfde4c96e9c79a4d2462e73c768fb6a2f7d43f`.
